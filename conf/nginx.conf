location PATHTOCHANGE {
	if ($scheme = http) {
		rewrite		^		https://$server_name$request_uri? permanent;
	}
	include					/etc/nginx/proxy_params;
	proxy_pass				SCHEMATOCHANGE://HOSTTOCHANGE:PORTTOCHANGEHOSTPATHTOCHANGE;
}
